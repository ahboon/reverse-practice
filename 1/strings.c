#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   char flag[88] = "flag{this_is_and_easy_peasy_strings_challenge}\0";
   printf("Smth is here, but i can't seem to print it");
   return 0;
}